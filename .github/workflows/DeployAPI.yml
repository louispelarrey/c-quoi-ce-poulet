name: API Server Deployment with doctl

on:
  push:
    branches:
      - dev
    paths:
      - 'thematic-api/**'
      - '.github/workflows/DeployAPI.yml'

jobs:

  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_TOKEN }}

      - name: Update docker compose image
        run: |
          doctl compute ssh root@${{secrets.SERV_IP}} --ssh-port 22 --ssh-command "cd /home/root/c-quoi-ce-poulet/thematic-api && git pull